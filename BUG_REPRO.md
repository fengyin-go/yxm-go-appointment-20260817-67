# Bug 是什么

创建排班、医生、患者时，字段校验错误被包装后无法被识别为校验错误，接口错误码错误地落到了服务器内部错误。

# 如何触发

调用创建排班接口时提交一个非法日期，例如 `date: "bad-date"`；创建医生或患者时提交缺失必填字段也可触发。

# 错误信息

接口返回 500，而不是 400；响应中显示 `create schedule: date: 日期格式需为 2006-01-02` 一类包装后的内部错误信息。
